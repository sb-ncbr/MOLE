<UserControl x:Class="WebChemistry.Tunnels.WPF.Controls.CSADatabaseControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:extToolkit="clr-namespace:Microsoft.Windows.Controls;assembly=WPFToolkit.Extended"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListBox x:Name="sitesList" ScrollViewer.HorizontalScrollBarVisibility="Hidden" Style="{DynamicResource NoScrollListBox}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <RadioButton GroupName="CSA" DataContext="{Binding}" IsChecked="{Binding Residues.IsSelected, Mode=OneWay}" Checked="RadioButton_Checked" Unchecked="RadioButton_Unchecked" >
                        <TextBlock><Run Text="{Binding Residues.ResidueString, Mode=OneTime}" /></TextBlock>
                    </RadioButton>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" x:Name="noEntry">No entry found.</TextBlock>
    </Grid>
</UserControl>
